# Copy to .env.local for local dev, or set in Cloudflare Pages → Settings → Environment Variables

# URL of the deployed mailer Worker (no trailing slash)
VITE_WORKER_URL=https://mailer-worker.gemma-serenity.workers.dev

# Must match ADMIN_SECRET secret set in the Worker
VITE_ADMIN_SECRET=your_32_char_hex_here
